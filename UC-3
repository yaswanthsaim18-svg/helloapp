public class Greeting {
    public static void main(String[] args) {
        String name;

        if (args.length > 0) {
            name = args[0];
        } else {
            name = "Besulelu";
        }

        System.out.println("Hello, " + name + "!");
    }
}
